#First App
apiVersion: v1
kind: Pod
metadata:
 name: treda.test.com
 labels:
  app: firstapp
spec:
 containers:
 - name: firstapp
   image: tomcat
   ports:
   - name: siteport
     containerPort: 3000